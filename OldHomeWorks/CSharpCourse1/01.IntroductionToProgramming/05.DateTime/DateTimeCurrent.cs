using System;

/*Create a console application that prints the current date and time.*/

class DateTimeCurrent
{
    static void Main()
    {
        Console.WriteLine(DateTime.Now); //DateTime.Now - Gets a DateTime object that is set to the current date and time on this computer, expressed as the local time.
    }
}